<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ChatApp"
             x:Class="ChatApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Top bar background -->
            <Color x:Key="NavBarBackgroundColor">black</Color>

            <!-- Title + Icon (hamburger/back) color -->
            <Color x:Key="NavBarForegroundColor">White</Color>

            <!-- Apply to Shell -->
            <Style TargetType="Shell">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource NavBarBackgroundColor}" />
                <Setter Property="Shell.TitleColor" Value="{StaticResource NavBarForegroundColor}" />
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource NavBarForegroundColor}" />
            </Style>

            <!-- Apply to Flyout background + text -->
            <Style TargetType="FlyoutItem">
                <Setter Property="FlyoutDisplayOptions" Value="AsSingleItem" />
            </Style>


            <Style TargetType="Label">
                <Setter Property="TextColor" Value="White" />
            </Style>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
